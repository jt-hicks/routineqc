% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporting_consistency.R
\name{filter_qc_facilities}
\alias{filter_qc_facilities}
\title{Filter a Review Cohort by Reporting Consistency}
\usage{
filter_qc_facilities(
  data,
  strictness = "all",
  max_prop_without_testing = NULL,
  max_gap_months = NULL,
  min_months_with_testing = NULL,
  min_prop_with_testing_dataset_window = NULL,
  consistency = NULL
)
}
\arguments{
\item{data}{A QC data frame with \code{facility_id}, \code{month_date}, and \code{tested}.}

\item{strictness}{A level name from \code{\link[=qc_consistency_levels]{qc_consistency_levels()}}.}

\item{max_prop_without_testing}{Optional threshold override.}

\item{max_gap_months}{Optional threshold override.}

\item{min_months_with_testing}{Optional threshold override.}

\item{min_prop_with_testing_dataset_window}{Optional threshold override.}

\item{consistency}{Optional precomputed output of
\code{\link[=summarise_qc_reporting_consistency]{summarise_qc_reporting_consistency()}} used as a measurement cache. The
strictness decision is always recomputed from the supplied thresholds.}
}
\value{
A filtered tibble preserving source columns and row order.
}
\description{
Keeps every row belonging to facilities that meet the selected
reporting-consistency strictness, and removes no rows from a facility that
passes. This is display-only cohort selection for review: it never changes a
stored run, never authorizes exclusion, and never edits an observation.
Excluding a facility here means its records were not reviewed under the
selected cohort, not that its records are wrong.
}
